# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
---
modules:
  - type: script
    snippets:
      - > # update /usr mtime to cause /etc/.updated to be updated
          set -euxo pipefail && 
          sudo touch -d @1 /usr
